// This assembly is intended solely for browser (WASM) contexts.
// All public APIs call browser-only JavaScript interop; they will throw
// at runtime if invoked outside a browser environment.
using System.Runtime.Versioning;

[assembly: SupportedOSPlatform("browser")]
